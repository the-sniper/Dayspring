# Orchestra charters

Edit these `.txt` files to change agent prompts.

`../charters.ts` only loads them. Do **not** paste charter prose (especially markdown ` ```json ` fences) into the `.ts` file — that breaks Turbopack parsing.
